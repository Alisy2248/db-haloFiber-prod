{
  "name": "my-json-server",
  "version": "1.0.0",
  "description": "Fake API using json-server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port ${PORT: 0.0.0.0}"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
