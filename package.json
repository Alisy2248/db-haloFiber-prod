{
  "name": "my-json-server",
  "version": "1.0.0",
  "description": "Fake API using json-server",
  "main": "index.js",
  "scripts": {
    "smart": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
